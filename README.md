# fivem_watcher
Python script to restart fivem packages with keybind. Pressing Ctrl + s will restart packages using sockets.

### install deps
`python -m pip install keyboard`

### usage
`python ./fivem_watch my_package_to_restart`